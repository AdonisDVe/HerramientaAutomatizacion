# === Base de Datos ===
DB_HOST=localhost
DB_USER=root
DB_PASSWORD=TU_PASSWORD_AQUI
DB_NAME=novum_qa_platform

# === Seguridad JWT ===
# Genera uno seguro: node -e "console.log(require('crypto').randomBytes(64).toString('hex'))"
JWT_SECRET=CAMBIA_ESTO_POR_UN_STRING_ALEATORIO_LARGO

# === Servidor ===
PORT=3001

# === CORS - URL del Frontend ===
FRONTEND_URL=http://localhost:4200

# === Dominio público del servidor (para URLs de evidencias) ===
SERVER_URL=http://localhost:3001
